using UnityEngine;
using System.Collections;

public class PlayerTextToSpeak : MonoBehaviour {
	public TargetController targetScript;
	//public PlayerActionController playerActionControllerScript;

	// Use this for initialization
	void Start () {
		this.renderer.sortingLayerName = "Dialogs";
		this.renderer.sortingOrder = 0;
	}
	
	// Update is called once per frame
	void Update () {
		//if(isPlayerSpeaking){
		//	this.guiText.text = 
		//}
	}
}
