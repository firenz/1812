using UnityEngine;
using System.Collections;

public class DisplayConversationText : MonoBehaviour {

	// Use this for initialization
	void Start () {
		this.renderer.sortingLayerName = "Dialogs";
		this.renderer.sortingOrder = 0;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
