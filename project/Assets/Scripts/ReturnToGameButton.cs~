using UnityEngine;
using System.Collections;

public class ReturnToGameButton : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		
	}

	void OnMouseOver(){
		if(Input.GetMouseButtonDown(0)){
			//Debug.Log("LastPlayableLevelID: " + GameStateManager.Instance.currentGameState.lastPlayableLevelID);
			//GameStateManager.Instance.ChangeScene(Application.loadedLevel, GameStateManager.Instance.currentGameState.lastPlayableLevelID);
		}
	}
}
